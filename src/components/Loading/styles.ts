import { makeStyles } from '@material-ui/core/styles'

export default makeStyles(() => ({
  loading: {
    height: '600px',
    display: 'flex',
    justifyContent: 'center',
    alignItems: 'center'
  }
}))
